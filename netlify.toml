[build]
  base = "."
  publish = "dist/public"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--prefix=. --silent"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200